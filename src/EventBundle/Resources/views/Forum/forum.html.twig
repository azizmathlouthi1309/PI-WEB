{% extends 'admin1.html.twig' %}
{% block event %}
    <!--
    <nav class="main-header navbar navbar-expand navbar-white navbar-light">
        <form class="form-inline ml-3" method="get" action="{{ path('event_search') }}">
            <div class="input-group input-group-sm">
                <input class="form-control form-control-navbar" type="search" placeholder="search..." name="motcle" aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-navbar" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
        </form>
    </nav>
    -->


    <section class="content">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <table id="example2" class="table table-bordered table-hover" >
                        <thead >
                        <tr align="left">
                            <th>Parent name</th>
                            <th>Event name</th>
                            <th>Message</th>
                            <th>Delete</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for e in forums %}
                            <tr>
                                <td>{{ e.getUser().getUsername() }}  </td>
                                <td>{{ e.getEvent().getName() }}  </td>
                                <td>{{ e.getMessage() }} </td>
                                <td> <a href="{{ path('forum_delete',{'id':e.id}) }}" ><button type="button" class="btn btn-primary" style="background-color : #B22222;  border-color:#B22222;">Delete</button> </a> </td>

                            </tr>
                        {% endfor %}
                        </tbody>

                    </table>
                </div>
            </div>
        </div>

        <div class="col-sm-12 col-md-7">
            <div class="dataTables_paginate paging_simple_numbers">
                <ul class="pagination">
                    <li class="pagnate_button page-item previous">
                        {{ knp_pagination_render(forums) }}
                    </li>
                </ul>
            </div>
        </div>
    </section>
{% endblock %}